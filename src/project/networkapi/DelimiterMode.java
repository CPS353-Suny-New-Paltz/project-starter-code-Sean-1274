package project.networkapi;

public enum DelimiterMode {

    DEFAULT,  // Use the system's predefined default delimiters (ignores any user-provided delimiters)
    CUSTOM    // Use the specific delimiter characters provided by the user in the request
}

